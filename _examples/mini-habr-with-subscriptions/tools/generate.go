package tools

// Regenerate this example against the local gqlgen (see the `replace` directive
// in go.mod). Run `go generate ./...` from anywhere in the module: gqlgen finds
// ../gqlgen.yml by walking up from this directory and chdirs to it before
// generating, so the schema paths in gqlgen.yml resolve correctly.
//
//go:generate go run github.com/99designs/gqlgen
