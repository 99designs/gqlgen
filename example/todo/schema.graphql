
schema {
	query: Query
	mutation: Mutation
}

type Query {
	todo(id: Int!): Todo
	lastTodo: Todo
	todos: [Todo!]!
}

type Mutation {
	createTodo(text: String!): Todo!
	updateTodo(id: Int!, done: Boolean!): Todo!
}

type Todo {
	id: ID!
	text: String!
	done: Boolean!
}
