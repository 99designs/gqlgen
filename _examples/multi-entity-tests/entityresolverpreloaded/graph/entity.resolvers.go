package graph

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.93-dev

import (
	"context"
	"entityresolverpreloaded/graph/model"
	"fmt"
	"strings"
)

// FindManyProductByIDs is the resolver for the findManyProductByIDs field.
//
// This is the whole point of preloaded_requires: every product's @requires field
// (Category) is already populated on the representation, so the resolver has a
// single scope containing all products' @requires data. The "display" choice
// below is computed once across the whole batch — standing in for a batched
// ML-inference call — which is impossible with the computed strategy (see the
// sibling entityresolvermulti example), where the @requires data reaches only a
// per-entity field resolver, one product at a time.
func (r *entityResolver) FindManyProductByIDs(ctx context.Context, reps []*model.ProductByIDsInput) ([]*model.Product, error) {
	// The whole batch is visible at once: collect every product's category.
	categories := make([]string, len(reps))
	for i, rep := range reps {
		categories[i] = rep.Category
	}
	batch := strings.Join(categories, ",")

	products := make([]*model.Product, len(reps))
	for i, rep := range reps {
		products[i] = &model.Product{
			ID:      rep.ID,
			Display: fmt.Sprintf("%s display (batch of %d: %s)", rep.Category, len(reps), batch),
		}
	}
	return products, nil
}

// Entity returns EntityResolver implementation.
func (r *Resolver) Entity() EntityResolver { return &entityResolver{r} }

type entityResolver struct{ *Resolver }
