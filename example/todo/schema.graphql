schema {
	query: MyQuery
	mutation: MyMutation
}

type MyQuery {
	todo(id: Int!): Todo
	lastTodo: Todo
	todos: [Todo!]!
}

type MyMutation {
	createTodo(text: String!): Todo!
	updateTodo(id: Int!, done: Boolean!): Todo!
}

type Todo {
	id: Int!
	text: String!
	done: Boolean!
}
