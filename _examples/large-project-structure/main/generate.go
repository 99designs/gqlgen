package main

// Regenerate this example against the local gqlgen (see the `replace` directive
// in go.mod). Run `go generate ./...` from this directory; gqlgen.yml lives here
// alongside it.
//
// The dependency on the gqlgen command is pinned in tools.go (build-tagged
// `tools`); the directive lives here because `go generate` ignores build-tagged
// files.
//
//go:generate go run github.com/99designs/gqlgen
