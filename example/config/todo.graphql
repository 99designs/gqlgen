type Todo {
  id: ID! @goField(forceResolver: true)
  databaseId: Int! @goField(tag: "json:\"databaseId\" db:\"id,pk\"")
  text: String!
  done: Boolean!
  user: User!
}

input NewTodo {
  text: String!
  userId: String!
}

